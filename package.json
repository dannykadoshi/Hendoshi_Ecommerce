{
    "name": "hendoshi-ecommerce",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "format:css": "prettier --write \"static/**/*.css\"",
        "prepare": "husky install"
    },
    "devDependencies": {
        "husky": "^8.0.3",
        "prettier": "^3.0.0"
    }
}
